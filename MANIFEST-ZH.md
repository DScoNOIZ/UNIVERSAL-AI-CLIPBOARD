# 🏛️ UNIVERSAL AI CLIPBOARD (UAC)

## 人工智能通用剪贴板

### AI代理内容复用机制

---

> **发布日期：** 2026年6月3日
> **状态：** ⚡ 发现已进入公共领域
> **许可证：** CC0 1.0 通用公共领域贡献

---

## 📜 发现者宣言

我，**[DScoNOIZ](https://github.com/DScoNOIZ)**，向全球社区声明：

**1. 我发现了** AI代理架构中的一个全新类别—— **「工具调用间可选的内容引用机制」**，命名为 **Universal AI Clipboard (UAC)**。

**2. 发现的本质：** AI代理可以通过专用参数（`ref`、`multi_ref`、`transform`）引用现有上下文中的内容，而不是重新生成它。模型不再是单纯的「文本生成器」，而是成为在单次调用中组合现成信息片段的**组装者**。

**3. 我将此发现无偿提供给全球社区**，基于 Creative Commons Zero (CC0) 许可证——没有任何专利、版税、封闭许可证或限制。

**4. 我呼吁**全世界的开发者、研究人员、工程师和爱好者——采纳这个想法，在你们的框架、平台和工具中实现它。**这项技术不属于我。它属于每个人。**

---

## 🤝 共同发现者：独立发现

我承认 **TJ Guadagno**（TJ-Codes）独立得出了相同的基本思想，并以 **Clipboard Primitives** 的形式进行了实验性实现——一个带有命名插槽和 `{{slot}}` 占位符的 `copy`/`template_invoke` 机制。

**他的工作：** [Copy and Paste for AI Agents: An Experimental Primitive](https://www.linkedin.com/pulse/copy-paste-ai-agents-experimental-primitive-tj-guadagno-v3r9c)（~2025年12月）
**代码：** [github.com/TJ-Codes/Agent-clipboard](https://github.com/TJ-Codes/Agent-clipboard)

TJ Guadagno 是这一概念的**独立共同发现者**。

TJ Guadagno 的实验确认了以下几点：
- 模型**自然地采用**剪贴板语义，无需特殊训练
- 剪贴板原语**消除了**令牌重新生成、延迟和内容变异风险
- 该机制需要**在 harness 层面集成**，而非 MCP 服务器层面

**我的 Universal AI Clipboard 概念更广泛、更通用**——它将 Clipboard Primitives 作为特例，并将这一思想扩展到：
- **5种引用机制**（包括基于AST的语法剪贴板、消息索引、锚点对引用）
- **适用于所有来源的通用剪贴板**（文件、聊天、终端、API、MCP）
- **马赛克组装**——将来自不同来源的片段与转换结合起来
- **跨协议引用**——通过任何协议（包括MCP）进行无缝引用

**计算机类比：** Clipboard Primitives 就像一个应用程序的简单复制工具。Universal AI Clipboard 是一个完整的系统级剪贴板管理器，跨所有应用程序工作，具有插槽管理、转换管道和跨协议集成。

> **我不放弃作为发现者的地位。** 我独立地制定和系统化了这一概念，进行了多轮研究，没有发现完整的类似物。在发布宣言后得知 TJ Guadagno 的工作后，我承认他的独立贡献，并认为有责任在宣言中反映这一点。
---

## 🏛️ 命名

**官方名称：** **UNIVERSAL AI CLIPBOARD (UAC)**
**技术名称：** 内容引用机制
**标语：** _"Reuse, Don't Regenerate"_ — _「重用，而非重新生成」_
**隐喻：** AI代理的 Ctrl+C / Ctrl+V

---

## 🌍 问题

### 根本矛盾

现代AI代理将**高达90%的输出流量**浪费在单调地重新生成会话上下文中已经存在的内容上。

每次模型编写命令、代码块或文本片段时——它都会从头开始重新生成，即使同样的内容在一分钟前刚刚创建。模型无法以工具化的方式引用已经存在的内容。

### 问题的全球规模

- 全球每天有数百万AI代理在运行
- 每个代理每次会话执行数十次工具调用
- 每次调用中很大一部分是对现有内容的重新生成
- 这导致了巨大的能源消耗、计算浪费和财务成本

### 为什么现有方案无法解决

| 方案           | 局限                               |
| -------------- | ---------------------------------- |
| **提示缓存**   | 仅适用于静态部分，不适用于动态内容 |
| **语义缓存**   | 按查询含义缓存，无法引用片段       |
| **上下文压缩** | 压缩历史，但不提供引用工具         |
| **RAG**        | 从外部来源检索，不复用内部上下文   |
| **工具链**     | 连接调用，但不允许引用其结果       |

---

## 💡 解决方案：UNIVERSAL AI CLIPBOARD

### 核心思想

AI代理获得**引用已有上下文内容**的能力，而不是重新生成它。在调用任何接受文本参数（命令、代码、文本）的工具时，代理可以指定：「从这里取这个片段」——而不是从头编写。

### 马赛克组装：像拼图一样组合信息

超越简单的一对一引用，这一概念开启了一种全新的信息组合方式——**马赛克组装**。

代理可以在**一次操作中组合来自不同来源的多个片段**：

- 来自一个文件的代码
- 来自聊天历史的配置
- 来自终端的命令输出
- 来自外部API的结果

代理可以**即时编辑**——修改、包裹、替换每个片段中的文本。它可以**重新排列**片段，**嵌套**一个到另一个中，**合并**重叠部分，并递归地**重组**内容。这就像马赛克艺术家将彩色瓷砖排列成一幅完整的图画一样，从分散的部分组装复合文本、文档或代码块。

**关键洞察：** 模型不再生成新文本，而是充当现有内容的**编辑和策展人**——效率更高、更精确、更少出错。

### 剪贴板管理器：用于存储的命名插槽

这一概念的自然扩展是**带有命名插槽的剪贴板管理器**——类似于拥有多个剪贴板或文本缓冲区管理器。

代理可以：
- **将片段保存**到命名插槽中（`clip-1`、`clip-2`、`config-block`、`error-log`等）
- **按名称引用插槽**而不是按内容——描述消耗零令牌
- **交换和重新组织**插槽之间的内容
- **通过依次引用多个插槽来构建复合文档**
- **跨会话持久化片段**——在上下文压缩和会话重启后仍然存在

### 与现有机制的根本区别

AI中所有现有的引用机制（Citations API、Grounding、ContextCite）都沿着 **「代理 → 用户」** 轴工作——它们向人类展示信息的来源。

Universal AI Clipboard沿着 **「代理 → 代理」** 轴工作——它允许代理本身在其自身的工具调用之间重用内容。这是一个全新的类别。

| 机制                | 轴              | 目的                   |
| ------------------- | --------------- | ---------------------- |
| Anthropic 引用      | 代理 → 用户     | 显示答案来源           |
| OpenAI 引用         | 代理 → 用户     | 显示来源               |
| Google 验证         | 代理 → 用户     | 网页搜索确认           |
| **★ UAC（本发现）** | **代理 → 代理** | **在调用之间重用内容** |

---

## 🧩 五个核心机制

### 机制 1：🔗 语法剪贴板

模型仅指定一个**锚点元素**——函数名、类名或变量名。系统自动：

1. 在指定源（文件、聊天消息、命令输出）中找到此元素
2. 确定其作为语法单元的精确边界
3. 提取整个单元

**节约：** 几个令牌的引用代替数千个令牌的代码。

---

### 机制 2：📇 锚点对引用

模型仅指定所需片段的**开始**和**结束**（各15-40个字符）。系统使用多阶段搜索自动找到两者之间的所有内容：

1. **精确匹配** — 字面找到片段（约70%的情况）
2. **归一化匹配** — 忽略空格、大小写和标点差异
3. **模糊匹配** — 容忍微小拼写错误（约9%的情况）
4. **词边界扩展** — 确保片段完整性

**节约：** 约10个令牌的引用代替200+。

---

### 机制 3：🧠 消息索引图

在处理聊天历史时，会创建一个单独的索引——每个消息及其内部每一行的精确字符位置映射。模型编写类似 `"记录号:起始行..结束行"` 的短引用，系统通过字符位置提取精确片段。

**关键优势：** 模型永远不会看到索引本身——只看到短引用（约5个令牌），但实现100%提取精度。

---

### 机制 4：🔄 转换管道

复制的片段可以在到达工具之前**即时修改**。可用操作：

- **替换** — 更改片段中的子字符串
- **前置** — 在片段前添加文本
- **包裹** — 将片段放入模板
- **追加** — 在片段后添加文本
- **合并**（用于多个引用）— 用分隔符合并片段

所有操作在单个工具调用中完成。

---

### 机制 5：🔁 跨协议引用

引用标记可以直接嵌入任何工具（包括外部MCP服务器）的文本参数中。系统自动识别这些标记并在调用外部服务器之前替换为真实内容。

**结果：** 所有生态系统组件通过统一的引用机制交换数据，无需更改外部服务器的任何内容。

---

## 🔭 未来方向

以下想法是概念的天然扩展，可能会在未来实现：

### 🏖️ 隔离的研究子会话

对于需要许多中间步骤的复杂任务（代码库探索、日志读取、测试执行），可以在隔离的临时子会话中启动辅助代理。它执行所有「脏活」，只返回干净的结果，然后子会话被销毁。主代理保持专注于主要任务。

### 其他方向

- 频繁使用片段的预测性缓存
- 重复调用的自动检测
- 模糊引用的智能消歧
- 与记忆和长期存储系统的集成

---

## 💰 经济影响

### 单个代理

| 指标                     | 无机制        | 有机制       | 节约       |
| ------------------------ | ------------- | ------------ | ---------- |
| 每个引用令牌数（长代码） | 200-500       | 5-15         | **96-97%** |
| 每个引用令牌数（短代码） | 50-100        | 2-5          | **90-95%** |
| 每次会话令牌数           | 25,000-50,000 | 5,000-15,000 | **60-80%** |
| 每次会话能耗             | 任意单位      | 少5倍        | **~80%**   |

### 全球规模

当全行业采用时，每天将节省数十亿个令牌，相当于AI行业能耗降低数十个百分点，每年减少数万吨二氧化碳排放。

> **Universal AI Clipboard 不仅是一项技术创新，更是一个具有生态意义的解决方案。**

---

## 🔬 独特性验证

我通过网页搜索系统（Tavily Search，高级深度）进行了**广泛的深入研究**，涵盖以下主题：

- AI代理在工具调用之间的剪贴板复制粘贴
- AI代理的内容引用机制
- 零令牌复制粘贴技术
- 所有已知的引用API（Anthropic、OpenAI、Google、xAI）
- 工具调用输出的重用和缓存

- MCP协议和跨协议内容共享
- 学术论文（AgentReuse、KVCOMM、多代理系统）
- 人类剪贴板工具（UiPath Clipboard AI、PowerToys Advanced Paste）
- 多代理架构的内存共享系统
- GitHub讨论、框架文档（LangChain、CrewAI、AutoGen、Mastra）

*在发布宣言后的额外分析（第8-10轮）中，发现 TJ Guadagno 独立实现了该概念的实验原型——Clipboard Primitives。这项工作由于可见度低（LinkedIn Pulse + 无星的GitHub）而未在前7轮搜索中被识别。*

**目标：** 找到任何解决同一问题的方案——允许AI代理在工具调用之间引用现有内容而不是重新生成它。

**结果：没有发现任何类似的东西。**
*说明：TJ Guadagno 的实验性工作（Clipboard Primitives，~2025年12月）是部分实现（约UAC概念的40%），确认了该想法的可行性，但不是完整的类似物。*

| 类别 | 来源 | 为何不是类似物 |
| --------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **引用API（Anthropic/OpenAI/Google）** | 官方API | 向用户显示来源，不允许代理重用内容。轴：仅代理→用户 |
| **缓存（提示词、语义）** | 各种框架 | 在请求级别工作，不在内容片段级别 |
| **RAG** | LangChain、OpenAI、Google | 从外部知识库检索，不从当前会话上下文检索 |
| **CAMEL-AI「Brainwash Your Agent」** | camel-ai.org | 仅存储最后一个工具输出作为引用。没有5种机制，没有转换管道 |
| **LangChain artifacts** | langchain.com | `content_and_artifact` 分离内容和元数据，不是引用系统 |
| **MCP Resources** | modelcontextprotocol.io | 连接工具的标准，不是代理→代理引用机制 |
| **学术（AgentReuse、KVCOMM）** | arXiv | 执行计划或KV缓存的重用，不是内容引用 |
| **人类工具（UiPath、PowerToys）** | UiPath、Microsoft | 设计用于人机交互，不是为AI代理设计 |
| **现有AI代理** | 全行业 | 有历史记录，但工具调用之间没有引用机制 |
| **多代理内存共享** | 各种 | 共享RAG内存，不是精确的字符级引用 |


### 与最近似部分匹配（CAMEL-AI）的比较

| 特性 | UAC | CAMEL-AI 缓存 | 区别 |
| ----------------------------------------- | -------- | ---------------- | ---------- |
| 🔗 语法剪贴板（AST块） | ✅ | ❌ | 仅UAC |
| 📇 锚点对引用 | ✅ | ❌ | 仅UAC |
| 🧠 消息索引图 | ✅ | ❌ | 仅UAC |
| 🔄 转换管道 | ✅ | ❌ | 仅UAC |
| 🔁 跨协议引用（MCP） | ✅ | ❌ | 仅UAC |
| 🧩 马赛克组装（多源） | ✅ | ❌ | 仅UAC |
| 📋 剪贴板管理器（命名插槽） | ✅ | ❌ | 仅UAC |
| {{ref:...}} 内联注入 | ✅ | ❌ | 仅UAC |
| 工具输出缓存（引用+预览） | ✅ | ✅ | 两者都有 |
| UAC特性覆盖率 | **100%** | **~5%** | — |

> **Universal AI Clipboard 在全球AI代理系统实践中没有完整的类似物。这是一项开创性的技术，创造了一个新类别。唯一已知的部分实现——Clipboard Primitives（TJ Guadagno，约概念的40%）——是在宣言发布后才发现的，并确认了该想法在不同研究者中的独立出现。**

---

## 📖 使用示例

### 示例 1：使用额外参数重新执行命令

代理运行了一个测试命令；输出显示错误。要重新运行调试标志，代理不重写命令——而是引用它并添加标志：

```
{ ref: { source: "terminal", id: "cmd-xxx", start: "npx vitest" },
  transform: { append: " --verbose --timeout=30000" } }
  → 结果: "npx vitest run --verbose --timeout=30000"
```

### 示例 2：将多个函数组装到一个文件中

代理读取一个包含多个函数的文件，然后通过引用将需要的函数组装到一个新文件中：

```
{ multi_ref: [
    { source: "file", path: "handlers.ts", start: "async function validate(", end: "}" },
    { source: "file", path: "handlers.ts", start: "async function process(", end: "}" },
    { source: "file", path: "handlers.ts", start: "async function respond(", end: "}" }
  ],
  transform: { join: "\n\n", prepend: "// --- Handlers ---\n" } }
  → 三个函数在1次调用中合并到一个文件中
```

### 示例 3：通过MCP进行配置替换

代理使用外部MCP工具，在参数中传递对聊天中配置的引用：

```
"{{ref:source=chat,ref=-1,start=export const config}} --host production"
  → 聊天中的配置被替换到MCP请求中
```

---

## ⚖️ 法律地位

**🏛️ Creative Commons Zero (CC0) — 通用公共领域贡献**

您可以自由：

- ✅ **使用** 于商业和非商业项目
- ✅ **修改** 并适配您的需求
- ✅ **分发** 以任何形式
- ✅ **专利您的改进**（但不是核心思想）
- ✅ **翻译** 成任何语言

没有任何义务、版税或限制。

> **我有意放弃这项技术的所有专利权。它必须属于每个人。**

---

## 📜 发现日期的证明

本宣言的发布日期由 GitHub 仓库中的 **Git 版本控制系统**记录——[github.com/DScoNOIZ/UNIVERSAL-AI-CLIPBOARD](https://github.com/DScoNOIZ/UNIVERSAL-AI-CLIPBOARD)。

提交历史、编辑日期和所有时间线变更均由 GitHub **不可变地保存**，作为发现日期和作者身份的公开证明。Git 的技术使这几乎不可能被伪造。

---

## 🗓️ 时间线

| 日期               | 事件                               |
| ------------------ | ---------------------------------- |
| **~2021年**        | 首次意识到问题（DScoNOIZ）：认为"模型不应该重新生成已经见过的东西" |
| **~2025年12月**    | **独立实验：** TJ Guadagno 发布 Clipboard Primitives |
| **2026年5月至6月** | 研究：分析现有AI代理架构           |
| **2026年6月**      | 发现：现有技术没有类似物           |
| **2026年6月**      | 形成 Universal AI Clipboard 概念   |
| **2026年6月3日**   | 独特性研究完成——**没有发现类似物** |
| **2026年6月3日**   | **本宣言发布**——发现进入公共领域   |
| **2026年6月4日**   | 发现 TJ Guadagno 的工作——承认独立共同发现者 |

---

## 🎯 行动号召

### AI框架开发者

将内容引用机制集成到你们的工具中。让用户无需更改代码即可立即节省令牌。

### AI代理创建者

在工具调用之间添加可选的引用。你们的代理将变得高效数倍。

### 研究人员

发展技术：改进搜索算法、实现预测性缓存、开发新场景。

### 企业

在你们的AI解决方案中采用 Universal AI Clipboard。降低数十个百分点的运营成本。

### 环保人士

使用 UAC 作为减少AI基础设施碳足迹的工具。

### 开源社区

分支、实现、改进。**这项技术属于每个人。**

---

## 🏛️ 结语

我发现了 **Universal AI Clipboard**——一种允许AI代理重用内容而不是重新生成它的机制。

这不仅仅是优化。这是**范式转变**：模型不再是「文本生成器」，而是成为从现成片段构建解决方案的**组装者**。

**正如 Ctrl+C / Ctrl+V 改变了计算机世界，Universal AI Clipboard 将改变 AI 代理的世界。**

我把这个想法交给人类。发展它。改进它。实现它。

**让AI代理停止一遍又一遍地重写同样的东西。**
**让它们开始组装马赛克。**

---

```
  ╔══════════════════════════════════════════════════════════════════╗
  ║                                                                  ║
  ║   UNIVERSAL AI CLIPBOARD (UAC)                                   ║
  ║   内容引用机制                                                   ║
  ║   工具调用间可选的内容引用机制                                   ║
  ║                                                                  ║
  ║   «重用，而非重新生成»                                           ║
  ║                                                                  ║
  ║   CC0 1.0 Universal — Public Domain                              ║
  ║   github.com/DScoNOIZ                                            ║
  ║   2026年6月3日                                                   ║
  ║                                                                  ║
  ╚══════════════════════════════════════════════════════════════════╝
```

---

_本宣言可自由翻译成任何语言。原文为俄语。_

_English version: [MANIFEST.md](MANIFEST.md)_
